import { createClient } from '@supabase/supabase-js'; import * as dotenv from 'dotenv'; dotenv.config({ path: './.env' }); const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_ANON_KEY); async function test() { const { data, error } = await supabase.from('profiles').upsert({ id: '11111111-1111-1111-1111-111111111111', name: 'Test', email: 'test@example.com', role: 'client' }).select(); console.log(error || data); } test();
